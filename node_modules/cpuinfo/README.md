# Nodejs Cpu Info (for linux)

This module read /proc/cpuinfo and provide all infos as an object

Ex.:

```
var cpuinfo = require('cpuinfo');

cpuinfo.on('update', function(d) { console.log(d); });
cpuinfo.update();
```

